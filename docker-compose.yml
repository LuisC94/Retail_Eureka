version: '3.8'

services:
  streamlit-buyer-agent:
    build:
      context: ./rl_buyer_interface
      dockerfile: Dockerfile
    container_name: streamlit-buyer-agent
    ports:
      - "8501:8501"
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
